
//* Check if the product is in stock */

    const CheckIfInStock = (inStock) => {
        if(inStock === true) {
            return true
        } else if(inStock === false) {
            return false
        }
    };

export default CheckIfInStock;